# cubepp

STM32 CMake Project Setup helper for C++ projects.

Install with:

```bash
pip install .
```

This package installs a `cubepp` console script. The `resources/` tree is included in the package and can be accessed from `cubepp/resources` after installation.
